#ifndef PMERGE_ME_HPP
#define PMERGE_ME_HPP

#include <algorithm>

template <typename Container> class PMergeMe {
public:
  void Sort(Container &c) {}

private:
  void SortImpl(Container &c) {
    // sort in pairs in Container<pair<T, T>>
    //
    // recursive call
    //
    // chain and pend
    //
    // insert using jacobsthal sequence
    //
    // overwrite c with chain
  }
};

#endif // PMERGE_ME_HPP
